# Studio connects to the CindersX Supabase project (stage first, then prod).
# The anon key is public-safe (RLS governs access); the service-role key is NEVER
# used here — it lives only in Supabase edge-function secrets.
VITE_SUPABASE_URL=https://skxodfgbzouosqdtvmdj.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key-here
# Note: the deploy-hook URL is NOT a client var — it lives as the Supabase secret
# DEPLOY_HOOK_URL on the trigger-rebuild edge function, so it never reaches the browser.
